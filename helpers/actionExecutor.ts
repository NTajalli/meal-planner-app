import { parseActionArgs } from "./parseActionArgs"; // Assuming this helper function parses args to an object

export async function executeAction(
  actionName: string,
  actionArgs: string
): Promise<void> {
  return;
}
